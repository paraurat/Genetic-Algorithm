package com.kleine.genetic.algorithm;

import com.kleine.genetic.algorithm.predatorandprey.Snake;

import java.util.List;

/**
 * Mutates offspring.
 */
public class Mutator {

    public List<Snake> mutate(List<Snake> snakes, float percentageOfMutations) {
        //TODO initialise the list
        List<Snake> mutatedSnakes = null;
        /**
         * TODO
         * - advanced
         * - Use the RandomHelper to mutate the chromosomes
         * - Only mutate percentageOfMutations
         */
        return mutatedSnakes;
    }
}
